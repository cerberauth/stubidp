name: stubidp
arch: ${GOARCH}
platform: linux
version: ${VERSION}
section: utils
priority: optional
maintainer: CerberAuth <hello@cerberauth.com>
description: |
  A mock OpenID Connect server for developers. Zero config, instant OIDC.
vendor: CerberAuth
homepage: https://github.com/cerberauth/stubidp
license: MIT

contents:
  - src: dist/binaries/stubidp-linux-${NFPM_ARCH}
    dst: /usr/local/bin/stubidp
    file_info:
      mode: 0755
